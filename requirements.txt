# Delta Chat Fabric — dependencies (baked at BUILD time, never runtime-pip).
# Exact versions are frozen by the GHCR CI lockfile (maintainer's image lane).
deltachat2>=1.0
deltachat-rpc-server>=1.147
deltachat-rpc-client>=1.147
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
pydantic>=2.6.0
pyyaml>=6.0
